#!/usr/bin/env ruby
# frozen_string_literal: true

require 'bundler/setup'
require 'classy'

require 'pry'

Pry.start

# dir = Classy.root.children.find { |c| c.path.to_path =~ /sample/ }
# pub = dir.children.find { |c| c.path.to_path =~ /public/ }
# priv = dir.children.find { |c| c.path.to_path =~ /secret/ }
